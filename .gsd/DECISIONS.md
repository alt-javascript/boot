# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001 | scope | Framework analysis scope | Spring Framework core + Spring Boot core only | User explicitly excluded non-core modules (WebMVC, Data, Security). Focus on IoC, DI, config, logging, lifecycle, events, AOP. | No | agent |
| D002 | M001 | arch | TypeScript stance | Pure JavaScript only | Ideological commitment — isomorphic flat ESM in browser without build step. Differentiator vs InversifyJS/NestJS/tsyringe. | No | agent |
| D003 | M001 | scope | Breaking changes | v3.0 breaking changes acceptable | Primary consumers are author's own projects. Quirks like null-matching autowire and duplicated global-ref code can be redesigned. | No | agent |
| D004 | M001 | scope | Jasypt exclusion | @alt-javascript/jasypt excluded from analysis | Not a formal part of Spring, standalone port, out of scope for core framework work. | No | agent |
| D005 | M001 | arch | Decorator approach | Must work without native decorators | Stage 3 decorators not in JS engines yet (Node 24 Symbol.metadata is undefined). Pure JS constraint means no transpiler fallback. | Yes — when decorators reach Stage 4 and ship in engines | agent |
| D006 | M001/S04 | arch | Monorepo structure | npm workspaces with @alt-javascript/common shared kernel | Prototype proves viability: 8 cross-package tests, eliminates 4 copies of global-ref code, enables coordinated versioning, breaks circular test deps. Each package stays independently publishable. | No | agent |
| D007 | M001/S04 | arch | AOP mechanism | JS Proxy — no subclassing or bytecode generation | Proxy is native, performant for non-hot-path use, and provides clean before/after/around semantics. PoC proves all 5 advice types work. | Yes — if performance benchmarks show issues in hot paths | agent |
| D008 | M001/S04 | arch | Event system | Custom isomorphic event bus (no Node EventEmitter) | Must work in browser ESM. Simple Map-based pub/sub is sufficient and portable. PoC proves typed events, wildcards, lifecycle events. | No | agent |
| D009 | M001/S04 | arch | Auto-discovery mechanism | Static __component class property | Most annotation-like pure JS approach. Co-located with class, no tooling. Consumer provides class array (no filesystem scanning — ESM has no classpath). PoC proves integration with ApplicationContext. | Yes — when native decorators ship, could offer decorator alternative | agent |
| D010 | M002/S01 | arch | Monorepo location | New repo with fresh git history | Clean break for v3.0. No git history baggage from 4 separate repos. Old repos archived. Package names stay the same — only repo structure changes. | No | agent |
| D011 | M005 | arch | JSDBC async model | All-async (Promise-based) | Idiomatic JS, works with all underlying drivers. Sync JDBC model doesn't translate to JS ecosystem. | No | agent |
| D012 | M005 | arch | JSDBC URL scheme | `jsdbc:subprotocol:` mirroring JDBC convention | Familiar to Java devs, extensible, driver self-registration via subprotocol. | No | agent |
| D013 | M005 | arch | JSDBC named params | `:name` syntax (matches Spring) | Spring-familiar, unambiguous, easy to parse. | No | agent |
| D014 | M005 | arch | JSDBC template naming | `JsdbcTemplate` / `NamedParameterJsdbcTemplate` | Own the name — JS port identity, not a fake JDBC. | No | agent |
| D015 | M005 | arch | JSDBC domain boundary | JSDBC repo = core interfaces + drivers only | Template layer belongs in boot/altjs monorepo where it integrates with CDI, config, lifecycle. | No | agent |
| D016 | M005 | arch | JSDBC browser driver | sql.js (Wasm SQLite) | 13.5k stars, broad compat, isomorphic SQL in browser. | No | agent |
| D017 | M005 | arch | JSDBC connection pooling | tarn.js (proven, Knex uses it in production) | Battle-tested, minimal API surface, well-maintained. | No | agent |
| D018 | M005 | arch | JSDBC CI test split | `npm test` = CI-safe; `npm run test:integration` = DB-dependent | pg/mysql/mssql/oracle tests need Docker. CI must pass without them. | No | agent |
| D019 | M006 | arch | Web/MVC adapter philosophy | Complement, don't replace | Thin bridge to Express/Fastify/etc. Same service layer, framework-specific controllers. | No | agent |
| D020 | M006 | arch | Controller convention | Static `__routes` metadata + imperative `routes(router)` | Declarative and imperative both supported. | No | agent |
| D021 | M006 | arch | Context propagation | Framework-native: `app.locals.ctx` (Express), `fastify.decorate('ctx')`, `request.ctx` (all) | Use each framework's idiom, don't fight it. | No | agent |
| D022 | M006 | arch | Lambda cold-start pattern | CDI boots once on first invocation, reuses via closure | Standard Lambda warm-reuse pattern. | No | agent |
| D023 | M006 | arch | Adapter landscape | 7 adapters: Express, Fastify, Koa, Hono, Lambda, CF Workers, Azure Functions | Covers all mainstream JS HTTP targets. Hapi excluded (declining). NestJS/Next.js excluded (different abstraction level). | Yes — add adapters as platforms emerge | agent |
| D024 | M007 | arch | Browser profile resolution | URL-to-profile mapping replaces WindowLocationSelectiveConfig | Declarative `{ profiles: { urls: { 'localhost:8080': 'dev' } } }` — symmetric with server-side NODE_ACTIVE_PROFILES. | No | agent |
| D025 | M007 | arch | Frontend adapter pattern | Framework-native bridges, no runtime dependency on target framework | Vue provide/inject, Alpine store, React Context/hooks, Angular providers. All testable without the framework. | No | agent |
| D026 |  | versioning | Monorepo version bump strategy | Bump all package versions together on every release, regardless of which packages changed | Simpler mental model — the monorepo has one version at any point in time. Eliminates the cognitive overhead of tracking which packages changed and updating cross-package dependency pins selectively. The cost of an unnecessary version bump is negligible; the cost of a missed or mismatched bump is debugging time. | No | human |
| D027 | M012 | versioning | Monorepo version bump strategy — lockstep versioning | Bump all package versions together on every release, regardless of which packages changed | Mirrors how large multi-module projects like Spring Framework and Apache Camel manage versions: the entire project moves as one version number. Developers can reason about compatibility instantly — if all packages show 3.0.6, they are compatible. No need to track which packages changed in which release or update cross-package dependency pins selectively. The cost (a few unnecessary npm publishes) is negligible; the benefit (zero version matrix confusion) is permanent. | No | human |
| D028 | M013 | arch | Middleware architecture for cross-cutting concerns across all server and serverless adapters | CDI component middleware pipeline using static __middleware = { order: N } convention, composed by MiddlewarePipeline.compose() in packages/boot | The onion/compose model (Koa-style) expressed as pure CDI components is the natural fit: symmetric with __routes, no new APIs, works identically across all 7 adapters via a shared normalised request shape. Middleware opts in via static __middleware = { order: N } — lower order = outermost. MiddlewarePipeline.collect() scans CDI context; compose() is pure functional. Three built-in middleware components (RequestLoggerMiddleware order:10, ErrorHandlerMiddleware order:20, NotFoundMiddleware order:30) are wired by all *Starter() functions and individually disableable via config flags. | Yes — order numbering convention may be refined; additional built-in middleware may be added | collaborative |
